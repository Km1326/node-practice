<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="/style.css">
  <title>Todo</title>
</head>
<body>
  <div>
    <a href="/" class="btn btn-1">Home</a>
    <div class="wrapper">
      <% if(data.length) { %> 
        <div class="show-todo">
          <h3 class="show-todo-title"><%= data[0].title %></h3>
          <p class="show-todo-desc"><%= data[0].description %></p>
        </div>
        <a class="btn btn-2" href=<%= '/todos/'+ data[0]._id + '/edit' %>>Edit</a>
        <form action=<%= '/todos/'+ data[0]._id + '/delete' %> method="GET" class='btn btn-2'>
          <input name="method" type="hidden" value="DELETE">
          <button type="submit" class="del__btn">Delete</button>
        </form>
      <% } else { %>
        <h3 class="red">  Data Not Available !!! </h3>
      <% } %> 
      </div>
  </div>
</body>
</html>